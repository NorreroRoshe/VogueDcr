
export interface IRange {
  minValue: number;
  maxValue: number;
}

export const DiametrRange: IRange = {
  minValue: 0,
  maxValue: 2000,
};

export const LampCountRange: IRange = {
  minValue: 0,
  maxValue: 100,
};

export const HeightRange: IRange = {
  minValue: 0,
  maxValue: 2000,
};

export const WidthRange: IRange = {
  minValue: 0,
  maxValue: 1500,
};

export const LengthRange: IRange = {
  minValue: 0,
  maxValue: 2000,
};

export const IndentRange: IRange = {
  minValue: 0,
  maxValue: 1500,
};

export const PriceRange: IRange = {
  minValue: 0,
  maxValue: 2000000,
};
